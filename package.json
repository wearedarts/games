{
  "name": "darts-games",
  "version": "0.0.0",
  "repository": "https://github.com/wearedarts/games.git",
  "license": "MIT",
  "scripts": {
    "build": "mkdir ./build && cp -R ./src/* ./build"
  }
}
